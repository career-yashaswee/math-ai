"use client";

import * as React from "react";
import * as BaseTooltip from "@/components/ui/tooltip";

export const TooltipProvider = BaseTooltip.TooltipProvider;
export const Tooltip = BaseTooltip.Tooltip;
export const TooltipTrigger = BaseTooltip.TooltipTrigger;
export const TooltipContent = BaseTooltip.TooltipContent;
