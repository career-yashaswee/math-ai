"use client";

import * as React from "react";
import * as BaseDialog from "@/components/ui/dialog";

/**
 * Design System Wrapper for Dialog components.
 */
export const Dialog = BaseDialog.Dialog;
export const DialogPortal = BaseDialog.DialogPortal;
export const DialogOverlay = BaseDialog.DialogOverlay;
export const DialogTrigger = BaseDialog.DialogTrigger;
export const DialogClose = BaseDialog.DialogClose;
export const DialogContent = BaseDialog.DialogContent;
export const DialogHeader = BaseDialog.DialogHeader;
export const DialogFooter = BaseDialog.DialogFooter;
export const DialogTitle = BaseDialog.DialogTitle;
export const DialogDescription = BaseDialog.DialogDescription;
